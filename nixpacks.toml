[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["./node_modules/.bin/prisma generate --schema=./prisma/schema.prisma"]

[start]
cmd = "node backend/src/server.js"
